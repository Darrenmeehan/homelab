# Proxmox Host Setup

## Setup SSH

- Create a non root user
- Disable login as root

## Terrform Setup

https://github.com/tfutils/tfenv

The version of Terraform required is specified in `.terraform-version`

Use of https://github.com/tfutils/tfenv makes changing to this version easier.
While use of tfenv is optional, the version of Terraform used is required.

- Create Terraform Specific User


